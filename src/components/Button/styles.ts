import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingTop: 14,
    paddingLeft: 24,
    borderRadius: 48,
    paddingRight: 24,
    paddingBottom: 14,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
